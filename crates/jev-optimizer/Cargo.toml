[package]
name = "jev-optimizer"
version = "0.1.0"
edition.workspace = true
description = "JevNQL optimizer: logical rewrites and logical -> physical planning"

[dependencies]
jevir = { path = "../jevir" }
