[package]
name = "jevnql-core"
version = "0.1.0"
edition.workspace = true
description = "JevNQL engine API: data registration, plan validation, optimization and execution"

[dependencies]
jevir = { path = "../jevir" }
jev-executor = { path = "../jev-executor" }
jev-optimizer = { path = "../jev-optimizer" }
jev-provider = { path = "../jev-provider" }
